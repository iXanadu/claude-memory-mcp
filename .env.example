# ha-semantic-memory API URL (default: http://localhost:8920)
# MEMORY_API_URL=http://localhost:8920

# Optional API token — must match HAMEM_API_TOKEN on the ha-semantic-memory server.
# Leave empty if the server has no token configured.
# MEMORY_API_TOKEN=

# Default memory scope: machine, shared, or project (default: machine)
# MEMORY_DEFAULT_SCOPE=machine
