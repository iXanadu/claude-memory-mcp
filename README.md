# claude-memory-mcp — archived

> **This project has been merged into [engram](https://github.com/iXanadu/engram)** —
> a general-purpose semantic memory service for AI agents (FastAPI + pgvector +
> in-process embeddings) with an inter-agent message bus. The MCP bridge that
> replaced this one lives in engram's `integrations/claude-code/`.

This repository is an archived redirect stub. Its history was removed when it
was retired (it contained internal development state files with no public
value).
